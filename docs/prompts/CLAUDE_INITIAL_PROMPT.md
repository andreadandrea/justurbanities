# Claude Code Initial Prompt

Read AGENTS.md first.

This is the Justurbanities HTML5 Canvas offline-first starter repository.

Do not use Phaser.

Use:
- TypeScript
- Canvas 2D
- HTML/CSS overlay UI
- IndexedDB via Dexie
- Service Worker + Cache API
- offline-first local saves
- sync queue for remote persistence

First, inspect the project and verify the current structure.

Then complete this task:
Implement the DialogueManager + QuestManager integration described in docs/ai-agents/CODEX_IMPLEMENTATION_PROMPT.md.

Before editing, summarize the files you plan to modify.
After editing, run:
npm run build
